{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<h1>📊 Dashboard</h1>

<p>Total Companies: <strong>{{ total }}</strong></p>

<h2>🧾 Status Distribution (dummy)</h2>
<ul>
    {% for user in users %}
        <li>{{ user.name }} – {{ user.siren }}</li>
    {% endfor %}
</ul>

<h2>💰 Revenue Chart</h2>
<canvas id="caChart" width="400" height="200"></canvas>

<script>
    const ctx = document.getElementById('caChart').getContext('2d');
    const chart = new Chart(ctx, {
        type: 'bar',
        data: {
            labels: {{ users|map(u => u.name)|json_encode|raw }},
            datasets: [{
                label: 'Revenu estimé (€)',
                data: {{ users|map(u => u.siren|slice(-2)|number_format(0, '', ''))|json_encode|raw }},
            }]
        }
    });
</script>
{% endblock %}
